# ============================================
# Copy Partner Proxy - Environment Configuration
# ============================================

# --------------------------------------------
# Application Environment
# --------------------------------------------
# Environment: development, staging, production
APP_ENV=development
NODE_ENV=development

# Logging level: error, warn, info, debug
LOG_LEVEL=info

# --------------------------------------------
# Server Configuration
# --------------------------------------------
# Host to bind the server to (0.0.0.0 for all interfaces)
APP_LISTEN_HOST=0.0.0.0

# Port to listen on
APP_LISTEN_PORT=8810

# --------------------------------------------
# CORS Configuration
# --------------------------------------------
# Regex pattern for allowed origins
# Examples:
#   - (.*) - Allow all origins (NOT recommended for production)
#   - ^https://example\.com$ - Allow only example.com
#   - ^https://(.*\.)?example\.com$ - Allow example.com and all subdomains
CORS_ALLOW_ORIGIN=(.*)

# --------------------------------------------
# AstraBit API Credentials (REQUIRED)
# --------------------------------------------
# Your AstraBit API key (provided by AstraBit)
ASTRABIT_API_KEY=your-api-key-here

# Your AstraBit API secret (provided by AstraBit)
ASTRABIT_API_SECRET=your-api-secret-here

# --------------------------------------------
# Copy Gateway Configuration
# --------------------------------------------
# URL of the copy-gateway service
# Examples:
#   - http://localhost:3360 (local development)
#   - http://copy-gateway:3360 (Docker)
#   - https://gateway.astrabit.com (production)
COPY_GATEWAY_URL=http://localhost:3360

# --------------------------------------------
# API Versioning Configuration
# --------------------------------------------
# Comma-separated list of supported API versions
API_SUPPORTED_VERSIONS=v1

# Default API version to use
API_DEFAULT_VERSION=v1

# JSON map of partner versions to gateway versions
# Example: {"v1":"v1","v2":"v2"}
API_GATEWAY_VERSION_MAP={"v1":"v1"}

# --------------------------------------------
# Authentication Configuration (Optional)
# --------------------------------------------
# Add your authentication-specific configuration here
# Examples:

# JWT Configuration (if using JWT authentication)
# JWT_SECRET=your-jwt-secret-here
# JWT_EXPIRES_IN=1h

# OAuth Configuration (if using OAuth)
# OAUTH_CLIENT_ID=your-client-id
# OAUTH_CLIENT_SECRET=your-client-secret
# OAUTH_ISSUER_URL=https://your-oauth-provider.com

# Database Configuration (if storing credentials in database)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# DATABASE_POOL_SIZE=10

# Redis Configuration (if using Redis for caching)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password
# REDIS_DB=0

# --------------------------------------------
# Security Configuration (Optional)
# --------------------------------------------
# Rate limiting configuration (requests per minute)
# RATE_LIMIT_TTL=60000
# RATE_LIMIT_MAX=100

# Request timeout (milliseconds)
# REQUEST_TIMEOUT=30000

# --------------------------------------------
# Monitoring & Observability (Optional)
# --------------------------------------------
# Sentry DSN for error tracking
# SENTRY_DSN=https://your-sentry-dsn

# Application version (for logging/monitoring)
# APP_VERSION=1.0.0
